# OULAD Behavior-Based Resource Access Prediction

This repository contains the starter code (skeleton) for a CS4451 project
that predicts whether a student will access learning resources in the
next module/week based on their historical behavior logs from the
Open University Learning Analytics Dataset (OULAD).

## Project Goal

Given:
- student click logs (studentVle)
- resource metadata (vle)
- student demographic information (studentInfo)

We aim to:
1. Construct weekly behavior features for each student.
2. Predict whether a student will access any resource in the next week.
3. Identify which behavioral features contribute most to next-week access.

## Dataset (OULAD)

Download the following raw CSV files and place them in `data/raw/`:
- `studentVle.csv`
- `vle.csv`
- `studentInfo.csv`
Open University Learning Analytics Dataset (OULAD)
https://analyse.kmi.open.ac.uk/open-dataset

## Running the Pipeline
python src/experiments.py


This will:
1. Load raw CSV data
2. Preprocess and merge tables
3. Build week-level features
4. Train baseline ML models
5. Evaluate performance
6. Save figures (confusion matrix, ROC curve, feature importance)

## Dependencies

Install all requirements:
pip install -r requirements.txt

## Notes

- This skeleton is intentionally lightweight to allow flexible extension.
- Additional visualizations or feature engineering can be added for the final report.
- All figures generated by the models will be stored in `figures/`.



